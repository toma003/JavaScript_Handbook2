// A1. Create an array of five city names. Print the first, the last (using at(-1)), and the length.

const cities = ["Paris", "Kyoto", "Venice", "Athens"];

console.log(cities[0]); 
console.log(cities.at(-1));
console.log(cities.length);
