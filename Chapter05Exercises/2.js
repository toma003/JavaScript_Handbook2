// 2. Write celsiusToFahrenheit(c) using the formula F = C × 9/5 + 32.

function  celsiusToFahrenheit(c) {

    return (c * (9/5)) + 32;
}

let c = 32;

console.log(`Celsius ${c}, Fahrenheit: ${celsiusToFahrenheit(c)}`);
